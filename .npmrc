# React Native / Expo tooling (Metro, autolinking) expects a flat node_modules
# layout, which pnpm's default isolated linker does not provide. Hoisting keeps
# the mobile app working in this workspace. Applies to the whole workspace.
node-linker=hoisted
