// Platform-agnostic shared surface consumed by both apps/web and the future
// React Native app. Keep this package free of any web- or server-only deps
// (no next/*, react-dom, "server-only", DOM globals, or Node APIs).
export * from "./types"
export * from "./genres"
